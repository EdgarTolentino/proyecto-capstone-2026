[project]
name = "gepp-core"
version = "0.1.0"
description = "Dominio puro de Guardian EPP: entidades, motor de reglas y agregador de eventos"
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.9,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "gepp_core"
module-root = "src"
